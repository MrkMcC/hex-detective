import DifficultyConfig from "../../../classes/DifficultyConfig";

interface Props {
  difficulty: DifficultyConfig;
}

const DifficultyBreakdown = ({}: Props) => {
  return <div>ContextInfo</div>;
};

export default DifficultyBreakdown;
